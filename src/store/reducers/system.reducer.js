const initialState = {
   
}
export function systemReducer(state = initialState, action) {
    var newState = state;
    switch (action.type) {

       

     
        default:
    }

    return newState;
}
